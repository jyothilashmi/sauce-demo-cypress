{
  "name": "sauce-demo-cypress",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "npx cypress open",
    "test:run": "npx cypress run",
    "merge-reports": "npx mochawesome-merge cypress/reports/*.json -o mochawesome.json",
    "generate-report": "npx marge mochawesome.json -f report -o cypress/reports",
    "test:report": "npm run test && npm run merge-reports && npm run generate-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.5.3",
    "cypress-mochawesome-reporter": "^4.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
